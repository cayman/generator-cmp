language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-cmp' ]; then cd .. && eval "mv $currentfolder generator-cmp" && cd generator-cmp; fi
